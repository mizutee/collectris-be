services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: collectrics
      POSTGRES_USER: collectrics
      POSTGRES_PASSWORD: collectrics
    ports:
      - "5432:5432"
    volumes:
      - collectrics-pg:/var/lib/postgresql/data

volumes:
  collectrics-pg:
